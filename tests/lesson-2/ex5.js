for (let i = 1; i <= 100 ; i++) {
	if ( i % 2 === 0) { // số chẵn
		console.log(`Số ${i} là số chẵn`); // in “Số <i> là số chẵn”
	}  else { // số lẻ
	console.log(`Số ${i} là số lẻ`); //in “Số <i> là số lẻ”
	}
}